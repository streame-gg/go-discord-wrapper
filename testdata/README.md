## Testdata

This data is used by tests with randomized values. 
The structs itself sometimes do not make sense, for example an Automoderation Rule does only send specific trigger metadata for specific trigger types.
